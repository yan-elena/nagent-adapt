institution_id : alice_inst .

status_functions:
states: detect(Who, What, How), design(Who, What, How), execute(Who, What, How).


constitutive_rules:

1: count-as detect(alice, n, watch(unfulfilled)) while unfulfilled(obligation(U, (order(N) & play(U, unit, _) & N > 0), O, D)) .

2: unfulfilled_count(n, Count) count-as design(alice, n, reassign(vl))  while Count>0 & Count<3.

3: unfulfilled_count(n, Count) count-as design(alice, n, modify(vl))  while Count>=3.

4: designed(OP, N1, new(Cond, Cons)) count-as execute(alice, N1, des(OP, new(Cond, Cons))) .



