scope main {

    norm n : started & play(U, unit, _)
        -> obligation(U, n, vl(X) & X<5, deadline)
    .

    norm n1 : detect(alice,n,count(unfulfilled, n))
        -> obligation(alice, n1, unfulfilled_count(n, C) & C > T, `never`)
     .

}