scope main {

    norm n : started & play(W, worker, _)
        -> obligation(W, n, vl(X) & X<5, `10 seconds`)
    .

    norm n1 : detect(alice, n, watch(unfulfilled_count))
      -> obligation(alice, n1, watch(unfulfilled_count(n)), `never`)
      .

}